# socketcom_basic_interactor
A basic HTML,CSS,Javascript application using electron which interacts with the socketcom application.

Require:
Node.js

Once code is checked out cmd into directory and run "npm install".

This will allow running the application using the command "npm start"

To package the executable use command "npm run make".
